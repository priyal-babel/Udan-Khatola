from tkinter import Toplevel

class Invoice:
    def __init__(self):
        window = Toplevel()
        # window = Tk()
        window.title("Invoice - Airline Reservation System")
        window.geometry("800x600")